version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: vaultsend
      POSTGRES_USER: vaultsend
      POSTGRES_PASSWORD: vaultsend
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
